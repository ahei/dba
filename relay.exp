#!/usr/bin/expect

set server [lindex $argv 0]
set user [lindex $argv 1]
set login [lindex $argv 2]
set expected [lindex $argv 3]
set host [lindex $argv 4]

spawn zssh $server -l $user
expect $expected
send "$login $host\r"
interact
